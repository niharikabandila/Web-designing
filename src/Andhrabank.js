import React from 'react'

export default function Andhrabank(props) {
    return (
        <div>
             <h1>Andhrabank is located at {props.location}  {props.data.name} </h1>
        </div>
    )
}
